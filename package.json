{
  "name": "ecommerce-fullpack",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --legacy-peer-deps --production=false --prefix backend && npm install --legacy-peer-deps --production=false --prefix frontend && npm install --legacy-peer-deps --production=false --prefix admin && npm run build --prefix frontend && npm run build --prefix admin",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
