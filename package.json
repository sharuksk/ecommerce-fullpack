{
  "name": "ecommerce-fullpack",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --legacy-peer-deps --prefix backend && npm install --legacy-peer-deps --prefix frontend && npm install --legacy-peer-deps --prefix admin && npm run build --prefix frontend && npm run build --prefix admin",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
