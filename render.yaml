services:
  - type: web
    name: ecommerce-backend
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: development
        scope: build
    branches:
      - main
